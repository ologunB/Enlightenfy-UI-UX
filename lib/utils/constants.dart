const endpoint = 'https://plentheon-event-api.herokuapp.com/api/v1';
const userDetails = 'user_details';
var userToken;

String presentDateTime() {
 // return DateFormat("EEE MMM d, HH:mm").format(DateTime.now());
}